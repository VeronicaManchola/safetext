API_KEY=
CORS_ORIGINS=*
MODEL_NAME_OR_PATH=./artifacts/model
THRESHOLD=0.5
MAX_LENGTH=256
MODEL_VERSION=

# combinación de evidencias
COMBINE_MODE=max           # opciones: max | noisy_or | weighted
URL_WEIGHT=0.5             # usado si COMBINE_MODE=weighted

GSB_API_KEY=
URL_REPUTATION_TIMEOUT_MS=2500